<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title>Add Friend</title>
	<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
</head>
<body>
	
	    <nav class="navbar navbar-default">
		  <div class="container-fluid">
		    <div class="navbar-header">
		      <a class="navbar-brand" href="/user">User Page</a>
		    </div>
		    <ul class="nav navbar-nav">
		      <li><a href="/user">Friend List</a></li>
		      <li class="active"><a href="/user/seachfriend">Seach Friend</a></li> 
		      <li><a href="/user/request">Request</a></li>  
		         
		    </ul>
		    <a href="/admin/signin" class="btn btn-success pull-right" style="margin-top: 10px;">Log out</a>
		  </div>
		</nav>
	<div class="col-lg-6">

		<div class="panel-heading">
                            <h1>Seach Friend</h1>
                        </div>
         <form id="seachfriendform" class="form-horizontal" role="form" method="POST" action="/user/seachfriend">

         	<div id="seachfriendalert" style="<% if(!data||!data.error) { %>display:none<% } %>" class="alert alert-danger">
                            <p>Error:</p>
                            <% if(data&&data.error) { %>
                                <span><%= data.error %></span>
                            <% } %>
                        </div>
                                     
         <div class="form-group input-group">
            <input type="text" class="form-control" name="email" placeholder="email">
            <span class="input-group-btn">
             	<button class="btn btn-success" type="submit">Seach</button>
            </span>
         </div>
        </form>
        <form id="addfriendform" class="form-horizontal" role="form" >
        	<div class="panel panel-default">
			 <div class="panel-heading">
                            Results
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Email</th>
                                            <th>First name</th>
                                            <th>Last name</th>
                                            
                                            <th>Add Friend</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                    <% if(data && data.users) { %>
                                    <% for(var i=0;i<data.users.length;i++) { %>
										<tr>
											<td><%= data.users[i].id %></td>
											<td><%= data.users[i].email %></td>
											<td><%= data.users[i].first_name %></td>
											<td><%= data.users[i].last_name %></td>
											
											<td><button class="btn btn-primary add_friend" id_friend="<%= data.users[i].id %>">Add Friend</button></td>
										</tr>
                                    <% } %>
                                    <% } %>
                                    </tbody>
                                </table>
                            </div>
                            <!-- /.table-responsive -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                    </div>
         </form>                     
        </div>
   <script type="text/javascript" src="/static/js/user.js"></script>
</body>
</html>