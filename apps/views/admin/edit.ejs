<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title></title>
	<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
</head>
<body>
	<div class="container">
		<nav class="navbar navbar-default">
		  <div class="container-fluid">
		    <div class="navbar-header">
		      <a class="navbar-brand" href="/admin">Admin Page</a>
		    </div>
		    <ul class="nav navbar-nav">
		      <li><a href="/admin">Users</a></li>
		      <li class="active"><a href="">Edit</a></li>     
		    </ul>
		    <a href="/admin/signin" class="btn btn-success pull-right" style="margin-top: 10px;">Log out</a>
		  </div>
		</nav>

	<div class="panel-body">
		<h3>Change infomations</h3>
                            <div class="row">
                                <div class="col-lg-6">
                                	<% if(data&&data.error) { %>
                                	<div id="Edit-alert" class="alert alert-danger col-sm-12"><%= data.error %></div>
                                	<% } %>
                                    <form role="form">
                                    	<input class="id" type="hidden" name="id" value="<% if(data&&data.user) { %> <%= data.user.id %> <% } %>">
                                        <div class="form-group">
                                            <label>Email</label>
                                            <input class="form-control email" name="email" type="text" placeholder="Enter email" 
                                            value="<%if(data&&data.user) { %><%=data.user.email%><% } %>">
                                            
                                        </div>
                                        <div class="form-group">
                                            <label>Password</label>
                                            <input class="form-control password" name="password" placeholder="Enter password" type="password" 
                                            value="<%if(data&&data.user) { %><%=data.user.password%><% } %>">
                                        </div>
                                        
                                        <div class="form-group">
                                            <label>First name</label>
                                            <input class="form-control first_name" name="first_name" placeholder="First name" type="text"
                                            value="<%if(data&&data.user) { %><%=data.user.first_name%><% } %>">
                                        </div>
                                        <div class="form-group">
                                            <label>Last name</label>
                                            <input class="form-control last_name" name="last_name" placeholder="Last name" type="text"
                                            value="<%if(data&&data.user) { %><%=data.user.last_name%><% } %>">
                                        </div>
                                        <div class="form-group">
                                            <label>Account</label>
                                            <input class="form-control account" name="account" placeholder="" type="number" min="1" max="3"
                                            value="<%if(data&&data.user) { %><%=data.user.account%><% } %>">
                                        </div>
                                        
                                        <button type="button" class="btn btn-success user_edit">Edit</button>
                                        <button type="reset" class="btn btn-default">Reset</button>
                                    </form>
                                </div>
                                <!-- /.col-lg-6 (nested) -->
                                
                                <!-- /.col-lg-6 (nested) -->
                            </div>
                            <!-- /.row (nested) -->
                        </div>
	</div>
	<script type="text/javascript" src="/static/js/user.js"></script>
</body>
</html>